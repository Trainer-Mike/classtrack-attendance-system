{
  "name": "trainer-class-attendance",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm install --prefix backend && npm run dev --prefix backend\" \"npm install --prefix frontend && npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
